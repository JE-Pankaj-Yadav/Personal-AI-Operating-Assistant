import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {ShieldCheck} from 'lucide-react'; import Logo from '../components/Logo'; import {useAuth} from '../stores/auth'; import {Toast} from '../components/Toast';
export default function Login(){const [mode,setMode]=useState<'login'|'register'>('login'),[email,setEmail]=useState(''),[username,setUsername]=useState(''),[name,setName]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''); const {login,register}=useAuth(); const nav=useNavigate(); const submit=async(e:React.FormEvent)=>{e.preventDefault();setErr('');try{if(mode==='login')await login({email,password});else await register({email,username,display_name:name,password});nav('/')}catch(e){setErr((e as Error).message)}}; return <div className="authPage"><div className="authCard"><Logo/><div className="authTitle">{mode==='login'?'Secure sign in':'Create your PA NEXUS account'}</div><p>Local-first AI control center with explicit provider and telemetry states.</p><form onSubmit={submit}>{mode==='register'&&<><label>Display name<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Username<input value={username} onChange={e=>setUsername(e.target.value)} required/></label></>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={10} required/></label><button className="primary wide" type="submit">{mode==='login'?'Sign In':'Create Account'}</button></form><button className="textBtn" onClick={()=>setMode(v=>v==='login'?'register':'login')}>{mode==='login'?'Create a new account':'Back to sign in'}</button><div className="securityNote"><ShieldCheck size={16}/> Session-protected · secrets never returned to the browser</div></div>{err&&<Toast message={err} onClose={()=>setErr('')}/>}</div>}
